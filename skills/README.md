# Skills in this repository

## `searxng-web-research`

Installable skill for agents to perform web research with this CLI:

- Search: `searxng-cli search <query>`
- Read: `searxng-cli read <url>`

Install:

```bash
bash skills/searxng-web-research/install.sh
```
